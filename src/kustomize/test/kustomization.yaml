apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- app
# because of the issues with - 9000=grpc-python/grpc-python-server-service:50051
# excluded this location temporarily
# see src/kustomize/base/cluster/ingress-controller/kustomization.yaml
# - cluster